# LeafFS 更新日志

作者：ewq2526。版本号格式：主版本.次版本.修订。此前没有任何版本或变更记录，本文件自 1.0.3 开始记录。

## 未发布

> 1.0.6 之后的修复与配置项，尚未发版。

### 修复

- **`NUL`、`CON` 这类名字不再被当成文件**：Windows 把这些保留名当设备，`os.path.exists` 对它们返回真，于是能混过「文件是否存在」的检查、一路走到缩略图流水线才出错 —— 同一个输入管理员看到 500、普通用户看到 404（没有泄露也没有越权，只是错误码不该这样分叉）。现在这类名字在路径入口就被拒绝；`NULL.txt`、`COM10`、`console` 这些正常名字不受影响。
- **并发登录尝试不再突破失败锁定**：锁定检查与失败计数分别在密码哈希计算的前后各加一次锁，中间那段时间没有保护，于是并发的多次尝试会**同时**看到「还没到上限」而全部放行 —— 单次突发可试次数从 6 涨到 20。现在判定与计数在同一把锁内完成，成功登录照旧清零计数。（由外部测试者复现：8 个并发请求 8 次全部通过。）
- **WebSocket 的删除请求什么都没删时会如实报失败**：这条消息读的字段是 `paths`，而漏传、传空列表、或者按 HTTP 那边的习惯写成 `files` 时，原来一律回「成功、删除 0 个」—— 调用方会以为自己要删的东西已经删掉了。现在这种情况明确回失败并说明原因。（该分支本身工作正常：字段名给对、路径合法时确实会删掉文件。）
- **配额的参数名写错时会明确报错，不再静默变成「不限」**：`/api/users/quota` 与 `/api/users/speed` 读的字段名分别是 `quota_mb` 与 `speed_kb`，而名字对不上或者漏传时，原来的写法会**静默取 0** —— 而 0 在这两个接口里是合法值（= 不限额／不限速），于是响应是「成功」，实际却把配额设成了不限。现在参数缺失或非法一律明确报错，并且不会改动任何设置。
- **文件夹显示的「几个文件 / 多少字节」不再卡住不动**：目录聚合统计的缓存**写入键与失效键形态不一致**（写入是 `users/zzw` 这种混合分隔符，失效时被归一化成 `users\zzw`），两者永不相等 —— 也就是说**失效从来没有真正生效过**。表现为：新传的文件不涨数（实测 240 秒、连传三次都停在旧值），管理员删掉账号后同名重建，新账号会看到**上一任**的文件数与占用空间。现在缓存键统一归一化，失效真正生效；启动时会自动丢弃按旧键存下的缓存条目并重新统计。
- **修掉缩略图缓存串号（一个账号会看到另一个账号的图）**：缩略图的落盘名原来是「相对路径把分隔符换成下划线」，而这个变换是**有损**的 —— `users/zzv/p_s.png` 与 `users/zzv_p/s.png` 会压成同一个文件。实测（外部测试者复现）：一个账号请求**自己的**文件，拿到的却是另一个账号的缩略图，而直接读取对方原图仍是 404 —— 权限判定本身没有问题，出问题的是取用缓存的那一步。现在落盘名改用路径哈希，不会再撞名；启动时会自动清掉旧命名的缩略图，之后按需重新生成。
- **认不出的 WS 消息类型不再静默丢弃**：此前已认证连接（游客、管理员）发来服务端不认识的类型时**完全没有应答**，客户端只能一直等（匿名连接反倒是有的回应的 —— 它在认证检查那一步就被拒了，走不到消息分发）。现在会回一条明确的错误，且连接保持可用。这条由外部测试者反馈。
- **清理 aria2c 时不再按进程名全杀**：此前用 `taskkill /f /im aria2c.exe`，会干掉这台机器上**所有** aria2c —— 包括另一个 LeafFS 实例（另一份安装或源码运行）的下载器；对方发现掉了会立刻重新拉起，两边就此互相抢杀。现在按本实例的 RPC 端口匹配，只清自己启动的那个。

### 配置

- 新增配置项 `guest_login_max_per_min`：游客登录的限速上限（单 IP 每分钟次数，默认 10）。可在管理页的「深度配置」中调整，也可直接写 `config/server_config.json`。

### 文档

- 安卓版随 APK 附带第三方组件声明与许可原文（APK 内 `assets/THIRD_PARTY_NOTICES.md` 与 `assets/licenses/`，共 23 份）—— 此前 APK 里**没有任何**第三方声明。

## 1.0.6 - 2026-09-19

> 本版新增安卓版，其余以安全加固与稳定性修复为主。

### 功能

- 新增安卓版：把 LeafFS 服务器跑在安卓手机上，App 启动后同一 WiFi 或热点下的其它设备用浏览器访问手机地址，即可浏览、预览、上传和下载文件，其它设备不必安装任何东西。
- 安卓版与桌面版共用同一套服务端与网页：界面、账号体系、配置格式与操作方式完全一致；两边的数据各自独立存放，互不同步。
- 手机上的数据（账号、共享文件、配置）保存在应用私有目录，卸载应用会一并清除，重要数据请另行备份。
- 新增文件分享：把自己目录里的文件与文件夹分享出去，分享的内容会作为“公共分享”出现在浏览页的分享区，别人也可以通过分享页 `/p/<分享者>` 直接访问。分享是引用而非复制，文件始终留在原来的位置。
- 分享可以设置访问码：设码后，未通过校验的人既看不到分享里的文件列表，也取不到文件内容；分享者本人以及未设码的分享不受影响。

### 安全

- 本版含一轮安全加固与权限修正，涉及访问控制、上传、会话与凭据、页面内容策略，以及内嵌窗口与手机端的外部访问封锁；出于安全考虑，此处不逐条展开。

### 修复

- 游客的搜索此前一律被拒，现在按角色返回其有权看到的结果。
- 上传额度此前会越用越满：传到一定量后所有上传都失败，删掉已上传的文件也不恢复，只能重启服务；现在额度随上传结束正常归还。
- 上传中的半成品此前会以怪名字出现在文件列表、搜索结果与文件夹大小统计里，现在完全不可见。
- 上传中途断开时，此前会把只传了一半的数据当成完整文件保存下来；现在不会保存，并明确报错。
- 上传过程中刷新或离开页面，此前的上传队列会静默卡住、后面的文件再也不传，也没有任何提示；现在会给出交代并把队列继续下去，桌面端在有文件正在上传时刷新还会先拦一下。
- 删除文件此前不管有没有真的删掉，界面一律提示“成功”；现在如实报告结果，并区分“文件被占用”与“权限不足”，部分成功时列表会立即刷新。
- 打包下载此前失败时仍旧回一个空压缩包并提示成功；现在明确报错。
- 公共分享目录此前每做一次文件操作就会从列表里消失，要重新打开页面才回来；现在一直都在。
- 服务重启后（手机端 App 被系统杀掉后重启也一样）此前所有人都会被登出；现在登录状态保留，同时登出、踢下线和删除账号依旧立即生效、不会在重启后复活。
- 被拒绝的请求此前在客户端常常只显示“网络错误”、看不到服务端给出的原因；现在能收到那条明确的错误提示。
- 文件选中态此前描边与左上角的勾经常对不上，现在两者始终同步。
- 删除账号时，该账号的家目录此前留在原处，现在会归档保留，同名重建拿到的是空目录。
- 深度配置里填入非法值或未知项，此前会提示保存成功却并不生效；现在会拒绝并说明原因。
- 此前非管理员的下载器页面会显示一个不起作用的设置区，现在既不显示也不下发相关配置。
- 崩溃或被强制结束后残留的临时上传文件此前会一直占用磁盘，现在在服务启动时清理。
- 证书提示页（8082）不再自动把人跳回主站：它此前只凭“这台浏览器登录过”就跳，而会话可能早已失效，结果是跳进浏览页又被弹回登录页，看起来像“点了一下又回来了”。现在该页只做说明，前往主站由页面上的按钮触发。

### 架构

- 主服务由“一个请求一条连接”改为连接复用，并补上空闲连接的主动回收；高级配置新增“空闲连接超时”（默认 15 秒，可调 1~300 秒）——打开页面时那一批静态资源不再逐个重新握手，也不会被浏览器挂着的空闲连接占满。
- 连接复用的配套：每个请求重新计算处理时长预算，上一个请求没有读完的内容必须先处理干净才允许复用，避免请求之间互相拖累或内容串位。
- 静态资源改为条件请求：资源没有变化时只回响应头，重复打开页面不再重传整包静态资源，同时改版依旧立即生效，不会看到旧页面。
- 桌面端内嵌浏览器内核由 4.2.2 升级到 6.2.1，站外访问的拦截由“定时检查当前地址”改为挂在浏览器内核的导航、网络与新窗口三层事件上，不再有最多 0.4 秒的闪现窗口。
- 目录列表的两条通道（网页请求与实时通道）合并为同一份实现，两者返回的内容不再出现差异。
- 缓存策略走统一出口：默认不允许缓存，只有确实该缓存的（静态资源、缩略图）才显式声明。
- 身份与权限类拒绝的返回码统一为 404（此前分散在 401 / 403 / 404 三种）。

## 1.0.5 - 2026-09-07

### 功能

- 用户信息页（我的）改版：自助修改密码（须验证原密码，修改后该账号其它设备下线）、列出本账号登录设备并一键退出其它设备、显示当前设备信息与空间用量（管理员为全站、普通用户为自己的目录、游客为公共目录）、服务器地址显示局域网访问地址并可一键复制。
- 界面语言与主题主色（蓝/绿/紫/橙/红）在“我的”页设置，选择同时保存到登录账号（服务端）；软件自带窗口等无痕会话在重启重新登录后仍按该账号的选择显示。浏览器带语言 cookie 时以各自选择为准。
- 全站支持更换主题主色，亮色与暗色各有一套配套背景配色。
- 英文模式下管理页等处的动态数据文案（统计、连接列表、状态指示、二维码说明等）随界面语言显示。

### 修复

- 修复启用 HTTPS 时偶发启动失败且无具体原因：TLS 上下文构建异常现在会记录原因。
- 修复全站与父目录的空间统计不随子目录变动刷新：目录大小缓存失效时同步失效其祖先聚合。
- “我的”页的服务器地址不再显示 localhost，改为显示局域网访问地址。

### 架构

- 代码结构重构：HTTP、WebSocket、推送、TLS、运行路径定位等按层拆分为独立模块，各功能控制器归入对应域包；对外接口、配置与数据文件保持不变。
- 新增自动化回归测试（tests/）：自动启动真实服务，覆盖登录、权限、上传下载、账号接口等关键路径。

## 1.0.4 - 2026-09-05

### 功能

- 新增多语言支持：界面提供中文与英文，语言在“我的”页选择，选择按浏览器保存（cookie `leaf_lang`）。
- 英文版采用“直写英文页面文件”方式：同名 `.en.html`，服务端按语言返回对应文件，无英文文件时回退中文；英文内容标注为 AI 翻译（AI-translated）。
- 新增统一翻译术语表 `TRANSLATION_GLOSSARY.md`，保证各英文页面译法一致。

### 修复

- 上传目标目录不存在时自动创建；一次上传没有任何文件保存成功时如实返回失败与原因，不再误报成功。
- WebSocket 上的删除、新建文件夹操作移入后台线程执行，删除大目录不再阻塞页面实时通信。
- aria2c（磁力/种子下载所用进程）异常退出或连不上时自动重启，页面状态随其变化刷新。
- 服务疑似卡死时自动取证：后台看门狗检测到请求停滞时，把全部线程调用栈写入 config/freeze_dump_*.txt，供排查。

### 文档

- CHANGELOG 自 1.0.3 记录基线起，将本轮多语言改动归入 1.0.4。

## 1.0.3 - 2026-09-05

### 安全

- HTTPS 证书：未配置证书时，首次启动自动生成随机自签服务器证书并写入 config；不生成 CA、不安装任何信任库，已配置证书时以配置为准。
- 修复 /browse/ 进入子文件夹一律 403：路径先做百分号解码再做权限校验。
- 修复扫码登录被 401 拦截：/api/qrlogin 加入无效会话白名单，扫码设备即使带失效 Cookie 也能完成登录。
- 证书提示页（8082）识别已登录设备：登录时附带非 Secure 的登录标记，已登录访问 8082 自动跳回 HTTPS 主站，登出同步清除。
- 默认口令改密提醒移入页面顶部横幅；会话与登录标记 Cookie 安全属性保持与传输层一致。

### 功能

- 新增用户信息页 /me（导航“我的”），显示当前账号与角色并提供退出登录；浏览、预览、下载器、管理及各管理子页统一加入口。
- 二维码颜色跟随亮暗主题（暗色为白点黑底），扫码登录弹窗的二维码加外框。
- 管理页：整机连接数、每设备 HTTP 连接数、每设备 WebSocket 连接数集中到"连接数上限"一张卡，保存立即生效。
- 高级配置新增 ZIP 打包方式开关，默认全流式（边压缩边发送），可切换为先打包后发送并显著提示磁盘占用风险。
- 深度配置移除与高级配置重复的运行参数，整机与单设备连接上限移出。
- 服务器日志预览改为最新在前，并按行高自适应铺满卡片。
- 全站文字允许选中复制。

### 修复

- 修复游客访问公共目录内子文件夹被拒绝的问题。
- WebSocket 每设备连接上限由固定值改为可配置项（配置键 ws_max_conn_per_ip，默认 8，管理页可调）。

### 文档

- README、分发版说明、第三方组件与许可声明、安装包许可文件更新至与 1.0.3 一致；标注 AI 辅助开发声明与第三方组件义务。
