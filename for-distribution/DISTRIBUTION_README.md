# LeafFS 使用说明（分发版）

LeafFS 是一个运行在局域网某台电脑上的文件分享/下载服务器。同一网络内的手机、电脑用浏览器打开服务器地址，即可浏览、预览、上传和下载文件，无需安装客户端。

本说明面向使用分发包（可执行文件或打包目录）的最终使用者。开发与许可细节见项目根的 `../README.md`；第三方组件声明与各组件许可原文在本目录（`THIRD_PARTY_NOTICES.md` 与 `licenses/`），**分发时请连同本目录一并提供**。

## 1. 快速开始

1. 启动程序（双击启动器，或按说明运行 `python -m leaffs`）。
2. 在程序启动提示里找到本机访问地址，用浏览器打开。
3. 服务端首次登录：打开启动提示中的本机一次性令牌链接，会以超级管理员身份自动登录（令牌一次性、仅本机有效），请借此进入管理界面。
4. 首次启动的超管账号 `admin` **没有口令**（不存在“默认弱口令”这回事）：登录后请立即在页面提示处设置一个口令；未设置时页面顶部会一直提醒。想用口令登录，口令得先由你自己设。
5. 同一网络内的其它设备，用浏览器访问 `http(s)://本机局域网IP:端口` 即可使用。

## 2. 默认端口

- 主站：8080。启用 HTTPS 后需用 https 访问。
- 证书提示页：8082（仅启用 HTTPS 时提供）。

端口可在管理页的高级配置中修改。

## 3. 常用入口

- 浏览：查看与操作文件。
- 分享：把自己目录里的文件与文件夹分享出去，可给分享设置访问码；分享内容会作为“公共分享”出现在浏览页，也可以把分享页地址直接发给别人。分享是引用而非复制，文件始终留在原位置。
- 预览：图片、视频等按类别浏览。
- 下载器：提交下载任务到服务器（HTTP、磁力、种子、m3u8）。
- 我的：当前账号信息与退出登录，并可在其中选择界面语言（中文 / English）。语言按浏览器记忆；英文界面为 AI 翻译，可能与中文存在差异。
- 管理：游客模式、用户与配额、连接上限、日志等设置。

## 4. 游客模式

- 默认关闭。开启后，任何人无需登录即可访问公共目录。
- 游客可以下载和上传新文件，但不能覆盖、删除文件或新建目录。
- 公共目录内不要放置私密文件。

## 5. 手机访问与证书提示

- 启用 HTTPS 且使用自动生成的自签证书时，手机浏览器会提示不安全，属正常现象。
- 确认地址是本机服务器后，按提示选择继续访问即可。
- 证书提示页 8082 会解释该现象。该页**不做任何自动跳转**，按页面上的按钮自行前往主站即可。

## 6. 常见问题

- 端口被占用：在管理页高级配置中改端口后重启。
- 忘记管理员口令：在本机重启服务，用启动提示中的本机一次性令牌链接以超管身份登录（每次启动都会生成新的本机令牌），到 管理 页的 用户管理 里改密码即可，无需删除文件；已登录时也可直接在 用户管理 修改。改密码在服务界面内完成，不提供独立脚本。
- 数据备份：备份 `config` 与 `shared_files` 两个目录即可。
- 页面文字均可选中复制；界面有亮暗两种主题。

## 7. 安全注意

- 本软件面向可信局域网，请勿直接暴露到公网。
- 请尽快为管理员账号设置口令（首次启动时它没有口令，只有本机一次性令牌能进去）。
- 连接与并发上限可在管理页调整；数值过大可能拖慢本机。

## 8. 声明

本项目为高度 AI 辅助开发的项目（主要使用 DeepSeek V4 Flash）。第三方组件、许可证与再分发说明见 `THIRD_PARTY_NOTICES.md`。作者对软件的正确性、安全性及适用性不作担保，亦不对使用造成的损失承担责任；请在可信网络内自行评估后使用。

作者：ewq2526（联系邮箱 ewq2526@163.com）。Copyright (c) 2026 ewq2526，许可见项目根的 `../LICENSE`。
